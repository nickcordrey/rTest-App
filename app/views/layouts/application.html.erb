<!DOCTYPE html>
<html>
<head>
  <title>rTest App</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults, "jquery-1.7.1", "raphael", "rtest" %>
  <%= csrf_meta_tag %>
 <style>
        g.node {
            font-family: Verdana, Helvetica;
            font-size: 12px;
            font-weight: bold;
        }
        circle.node-dot {
            fill: lightsalmon;
            stroke: red;
            stroke-width: 1px;
        }

        path.link {
            fill: none;
            stroke: gray;
        }
    </style>
</head>
<body>

<%= yield %>

</body>
</html>
